from setuptools import find_packages, setup

if __name__ == "__main__":
    setup(
        name="python-bidi",
        version="0.6.6",
        packages=find_packages(),
        description="Spoof implementation of python-bidi for xhtml2pdf",
    )
